# 🚀 Развертывание Bybit Trading Bot на VDS

Подготовлено всё необходимое для развертывания на сервере **45.8.251.77**

## 📋 Что создано

1. **[DEPLOY_QUICK_START.md](DEPLOY_QUICK_START.md)** - Быстрый старт (краткая инструкция)
2. **[deploy_vds.md](deploy_vds.md)** - Подробная пошаговая инструкция
3. **[deploy.sh](deploy.sh)** - Автоматический скрипт развертывания
4. **[upload_and_deploy.bat](upload_and_deploy.bat)** - Windows скрипт для загрузки на сервер

## ⚡ Быстрый старт (2 способа)

### Способ 1: Автоматический (рекомендуется)

#### С Windows (текущая машина):
```cmd
upload_and_deploy.bat
```

Этот скрипт автоматически:
- Скопирует deploy.sh на сервер
- Запустит процесс развертывания
- Проведет через все шаги установки

#### Напрямую с сервера:
```bash
# Подключитесь к серверу
ssh root@45.8.251.77

# Скачайте deploy.sh (если он уже в репозитории GitHub)
curl -o deploy.sh https://raw.githubusercontent.com/iradiomed33/bybit-trading-bot/main/deploy.sh
chmod +x deploy.sh
bash deploy.sh
```

### Способ 2: Ручная установка

Следуйте пошаговой инструкции в [deploy_vds.md](deploy_vds.md)

## 🔑 Важно: Что потребуется

### 1. SSH ключ для GitHub
Скрипт автоматически создаст SSH ключ. Вам нужно будет:
1. Скопировать публичный ключ (скрипт покажет)
2. Добавить его на https://github.com/settings/keys

### 2. API ключи Bybit
После развертывания отредактируйте файл `.env`:
```bash
nano /opt/bybit-trading-bot/.env
```

Замените:
```env
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here
BYBIT_TESTNET=false  # или true для тестовой сети
```

## 📦 Что установит скрипт

- ✅ Python 3.11
- ✅ Git
- ✅ Виртуальное окружение Python
- ✅ Все зависимости из requirements.txt
- ✅ Systemd сервис для автозапуска
- ✅ Настройка конфигурации

## 🎯 После развертывания

### Запуск бота
```bash
systemctl start bybit-api.service
```

### Проверка статуса
```bash
systemctl status bybit-api.service
```

### Просмотр логов
```bash
journalctl -u bybit-api.service -f
```

### Доступ к веб-интерфейсу
Откройте в браузере: **http://45.8.251.77:8000**

## 🔧 Управление

```bash
# Остановить
systemctl stop bybit-api.service

# Перезапустить
systemctl restart bybit-api.service

# Обновить код
cd /opt/bybit-trading-bot
git pull
systemctl restart bybit-api.service
```

## 📊 Мониторинг

```bash
# Логи в реальном времени
journalctl -u bybit-api.service -f

# Последние 100 строк логов
journalctl -u bybit-api.service -n 100

# Проверка API
curl http://localhost:8000/api/status

# Проверка извне
curl http://45.8.251.77:8000/api/status
```

## 🐛 Troubleshooting

### Сервис не запускается
```bash
# Проверить логи
journalctl -u bybit-api.service -n 50

# Проверить синтаксис Python
cd /opt/bybit-trading-bot
source venv/bin/activate
python -m py_compile run_api.py
```

### Порт занят
```bash
# Найти процесс на порту 8000
lsof -i :8000
netstat -tuln | grep 8000
```

### Проблемы с Git
```bash
# Проверить SSH подключение к GitHub
ssh -T git@github.com

# Должно быть: "Hi iradiomed33! You've successfully authenticated"
```

## 🔒 Безопасность

После развертывания рекомендуется:

1. **Настроить firewall:**
```bash
ufw allow 22/tcp
ufw allow 8000/tcp
ufw enable
```

2. **Создать резервную копию .env:**
```bash
cp /opt/bybit-trading-bot/.env /root/.env.backup
```

3. **Настроить регулярные обновления системы**

4. **Использовать сильные API ключи с ограниченными правами**

## 📞 Дополнительная помощь

Если возникли проблемы, проверьте:
- **[DEPLOY_QUICK_START.md](DEPLOY_QUICK_START.md)** - FAQ и частые проблемы
- **[deploy_vds.md](deploy_vds.md)** - Подробное описание каждого шага

## 🎉 Готово!

После успешного развертывания бот будет:
- ✅ Работать как системный сервис
- ✅ Автоматически запускаться при перезагрузке сервера
- ✅ Доступен через веб-интерфейс на порту 8000
- ✅ Логировать все действия в systemd journal

**Адрес веб-интерфейса:** http://45.8.251.77:8000
