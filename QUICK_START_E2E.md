# 🚀 TASK-QA-UI-SETTINGS-001: Quick Start Guide

## Что это?

E2E тесты которые **доказывают** что настройки UI влияют на бота.

Если UI позволяет менять leverage с 5 на 10, тест проверит что:
1. ✅ Значение сохранилось в API
2. ✅ Бот видит новое значение в runtime  
3. ✅ Бот использует 10x при формировании ордера (не хардкод 5x!)

## Самый быстрый способ запустить

### Windows:
```cmd
.\run_e2e_tests.bat
```

### Linux/Mac:
```bash
chmod +x run_e2e_tests.sh
./run_e2e_tests.sh
```

Скрипт автоматически:
- Установит Node.js зависимости
- Установит Playwright браузеры
- Запустит все тесты

## Ручной запуск (если нужен контроль)

### 1. Установка
```bash
cd tests/e2e
npm install
npx playwright install chromium
```

### 2. Запуск API (в отдельном терминале)
```bash
python run_api.py
```

Дождитесь пока API поднимется: `http://localhost:8000`

### 3. Запуск тестов

**Headless (без UI браузера):**
```bash
cd tests/e2e
npm test
```

**С видимым браузером:**
```bash
npm run test:headed
```

**Интерактивный режим (рекомендуется для отладки):**
```bash
npm run test:ui
```

**Отдельный test suite:**
```bash
npm run test:settings        # TC1: Basic settings
npm run test:advanced        # TC2: Advanced settings  
npm run test:validation      # TC4: Validation
```

### 4. Посмотреть отчёт
```bash
npm run report
```

Откроется HTML отчёт с:
- ✅/❌ для каждого теста
- Screenshots при ошибках
- Видео записи провалившихся тестов
- Подробные логи

## Что делать если тест упал?

### 1. Смотрим отчёт
```bash
cd tests/e2e
npm run report
```

### 2. Смотрим screenshot
В отчёте кликаем на провалившийся тест → видим screenshot момента ошибки

### 3. Смотрим trace (детали)
```bash
npx playwright show-trace test-results/path-to-trace.zip
```

### 4. Запускаем в debug mode
```bash
npm run test:debug
```

Откроется Playwright Inspector где можно:
- Пошагово идти по тесту
- Инспектировать элементы
- Видеть network запросы
- Смотреть console logs

## Что означают тесты?

### TC1: Basic Settings влияют на config
**Что проверяет:**
- Меняю leverage в UI с 5 на 7
- Жму Save
- Тест проверяет что `/api/bot/effective-config` отдаёт 7, не 5

**Если падает** → бот игнорирует UI и использует хардкод.

### TC2: Advanced Settings влияют на order intent
**Что проверяет:**
- Меняю ATR multiplier с 1.5 на 1.8
- Запускаю один тик бота (dry-run)
- Тест проверяет что SL рассчитан с mult=1.8

**Если падает** → advanced настройки не доходят до торговой логики.

### TC4: Validation блокирует невалидные значения
**Что проверяет:**
- Пытаюсь установить leverage=0 или отрицательный risk
- Тест проверяет что сохранение заблокировано
- Config остался прежним

**Если падает** → можно сломать конфиг невалидными данными.

## Интеграция в CI

Тесты автоматически запускаются в GitHub Actions при PR.

Чтобы увидеть результаты:
1. Открой PR
2. Зайди в "Checks" → "E2E Tests"
3. Смотри результаты

При провале:
- Скачай artifacts (screenshots, videos)
- Смотри что сломалось

## Troubleshooting

### "Server not ready"
- Убедись что `python run_api.py` работает
- Проверь что порт 8000 свободен
- Дай серверу больше времени (увеличь timeout в `playwright.config.ts`)

### "Element not found"
- Проверь что data-testid существует в HTML
- Используй `npx playwright codegen http://localhost:8000` для генерации селектора

### "Test timeout"
- Увеличь timeout в конфиге
- Проверь что бот не зависает
- Посмотри логи API

### Тесты проходят но я не верю
- Запусти `npm run test:headed` чтобы видеть браузер
- Добавь `page.pause()` в тест для остановки
- Посмотри network tab в trace viewer

## Расширение

### Добавить проверку новой настройки

1. **Добавь data-testid в HTML:**
```html
<input id="myNewSetting" data-testid="settings-my-new-setting" />
```

2. **Добавь тест:**
```typescript
test('должен применить myNewSetting', async ({ page, request }) => {
  await page.fill('[data-testid="settings-my-new-setting"]', '42');
  await page.click('[data-testid="settings-save-button"]');
  
  const config = await request.get('/api/bot/effective-config');
  const data = await config.json();
  
  expect(data.data.config.my_section.my_new_setting).toBe(42);
});
```

3. **Запусти:**
```bash
npm test
```

## Полезные команды

```bash
# Обновить скриншоты (если UI поменялся но это ОК)
npm test -- --update-snapshots

# Запустить только один файл
npx playwright test settings.spec.ts

# Запустить только один тест
npx playwright test -g "должен сохранить leverage"

# Показать браузер + замедлить
npx playwright test --headed --slow-mo=1000

# Генерировать новые тесты с записью
npx playwright codegen http://localhost:8000
```

## FAQ

**Q: Зачем это нужно?**  
A: Чтобы быть уверенным что UI не просто "красиво показывает", а реально управляет ботом.

**Q: Тесты медленные?**  
A: ~30-60 секунд на все 3 suite. Это нормально для E2E.

**Q: Можно ли запустить без API сервера?**  
A: Нет, тесты требуют живой API. Но можно использовать mock server.

**Q: Нужен ли реальный Bybit аккаунт?**  
A: Нет! Тесты в dry-run режиме, не размещают реальные ордера.

**Q: Как часто запускать?**  
A: Автоматически при PR (CI). Локально — когда меняешь UI/config/bot logic.

## 🎉 Готово!

Если всё работает — увидишь:

```
✅ TC1: Basic Settings → Runtime Config
✅ TC2: Advanced Settings → Order Intent  
✅ TC4: Settings Validation

Ran 10 tests: 10 passed, 0 failed
```

Теперь можно быть уверенным что UI настройки **реально** влияют на бота! 🚀
