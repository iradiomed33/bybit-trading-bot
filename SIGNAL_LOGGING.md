# 🔍 Логирование Сигналов - Полное Руководство

## ⚠️ ВАЖНО: Как запустить бота для генерации сигналов

**Dashboard - это ТОЛЬКО панель управления, но НЕ запускает реальную торговлю!**

### Нужно открыть ДВА терминала:

#### Терминал 1: API Dashboard
```bash
python run_api.py
```
Это запустит веб-панель на http://localhost:8000

#### Терминал 2: Trading Bot (ВАЖНО!)
```bash
# Paper mode (безопасный режим для тестирования)
echo "NO" | python cli.py paper
```

ИЛИ для live mode на реальные деньги:
```bash
# Live mode (реальная торговля - ОПАСНО!)
echo "YES" | python cli.py live
```

**ИЛИ просто запустите:**
```bash
start_trading_bot.bat
```

---

## ✅ Как проверить что логирование работает

1. **Откройте Dashboard:**
   - Перейдите на http://localhost:8000
   - Вкладка "Логи Сигналов"

2. **Нажмите "Обновить"**

3. **Вы увидите:**
   ```
   🔍 DEBUG | Category=market_analysis | {"symbol": "BTCUSDT", "last_close": 75011.3, "no_signal_reason": "No strategy triggered"}
   ```

**Это НОРМАЛЬНО!** Бот работает и логирует каждый анализ рынка.

4. **Когда появятся сигналы, вы увидите:**
   ```
   ✅ SIGNAL GENERATED | Strategy=TrendPullback | Symbol=BTCUSDT | Direction=BUY | Confidence=0.65
   ```

---

## 📊 Что добавлено

Добавлена **полная система логирования сигналов** для отладки случаев, когда ордера не открываются.

### Компоненты

1. **signal_logger.py** - Специализированный логгер для сигналов
2. **API эндпоинт** `/api/signals/logs` - Получение логов через API
3. **Вкладка Dashboard** - "Логи Сигналов" для просмотра в реальном времени
4. **Детальное логирование условий** - Каждый фильтр/условие стратегии логируется отдельно!

### 📋 Смотрите также:
- **[FILTER_CONDITIONS_LOGGING.md](FILTER_CONDITIONS_LOGGING.md)** - Когда и ПОЧЕМУ не выполняются условия стратегий

---

## 🚀 Как использовать

### 1. **Запуск сервера с логированием**

```bash
python run_api.py
```

Сервер автоматически начнет логировать все сигналы в файл:
```
logs/signals_2026-02-04.log
```

### 2. **Просмотр логов в Dashboard**

Откройте страницу и перейдите на вкладку **"Логи Сигналов"** 📊

Вы увидите:
- ✅ **Принятые сигналы** (зеленые)
- ❌ **Отклоненные сигналы** (красные) с причинами
- ⏳ **Выполнение ордеров** (желтые)
- 🔍 **Отладочную информацию** (голубые)

### 3. **Фильтрация логов**

Вкладка поддерживает фильтрацию:
- **Все логи** - вся информация
- **Только успешные** - только ✅
- **Только отклоненные** - только ❌
- **Только ошибки** - только критические

---

## 📋 Что означает каждый логе

### ✅ SIGNAL GENERATED
**Стратегия сгенерировала сигнал**

```
✅ SIGNAL GENERATED | Strategy=TrendPullback | Symbol=BTCUSDT | 
Direction=BUY | Confidence=0.65 | Price=43250.50
```

**Что это значит:**
- Стратегия нашла условия для покупки
- Уверенность: 65%
- Ордер еще не создан (могут быть фильтры)

**Следующий шаг:**
- Если видите это, но нет "SIGNAL ACCEPTED" → сигнал отклонен фильтром

---

### ✅ SIGNAL ACCEPTED
**Сигнал прошел все проверки и ордер открыт**

```
✅ SIGNAL ACCEPTED | Strategy=TrendPullback | Symbol=BTCUSDT | 
Direction=BUY | Confidence=0.65 | OrderID=12345678
```

**Что это значит:**
- Сигнал прошел ВСЕ проверки
- Ордер успешно создан
- OrderID = номер ордера на бирже

**Следующий шаг:**
- Смотрите "ORDER EXEC SUCCESS" для подтверждения выполнения

---

### ❌ SIGNAL REJECTED
**Сигнал отклонен с указанием причины**

```
❌ SIGNAL REJECTED | Strategy=TrendPullback | Symbol=BTCUSDT | 
Direction=BUY | Confidence=0.65 | 
Reason=Spread filter check failed | Details={"spread": 5.37, "limit": 2.0}
```

**Возможные причины:**

| Причина | Что делать |
|---------|-----------|
| `Spread filter check failed` | Спред слишком большой. Уменьшите лимит спреда в Settings |
| `Volatility filter check failed` | Волатильность слишком высокая. Выключите фильтр для тестов |
| `Orderbook depth check failed` | Недостаточная глубина книги заявок. Ордер слишком большой |
| `No sufficient balance` | Баланса недостаточно. Пополните счет |
| `Position size exceeds limit` | Размер позиции больше лимита. Уменьшите % риска |
| `Leverage too high` | Плечо слишком высокое. Уменьшите в Settings |

---

### ⏳ ORDER EXEC START
**Начало выполнения ордера**

```
⏳ ORDER EXEC START | Symbol=BTCUSDT | Direction=BUY | 
Qty=0.05 | Price=43250.50
```

**Что это значит:**
- Система отправляет ордер на биржу
- Ожидаем подтверждения

---

### ✅ ORDER EXEC SUCCESS
**Ордер успешно выполнен**

```
✅ ORDER EXEC SUCCESS | Symbol=BTCUSDT | Direction=BUY | 
OrderID=12345678 | FilledQty=0.05 | FilledPrice=43250.50
```

**Что это значит:**
- Ордер полностью выполнен
- Позиция открыта

---

### ❌ ORDER EXEC FAILED
**Ордер не выполнен**

```
❌ ORDER EXEC FAILED | Symbol=BTCUSDT | Direction=BUY | 
Reason=Insufficient balance | Error=Account balance too low
```

**Возможные причины:**

| Причина | Решение |
|---------|--------|
| `Insufficient balance` | Пополните счет |
| `API error` | Проверьте ключи API |
| `Network error` | Проверьте интернет |
| `Order cancelled` | Биржа отклонила ордер (параметры невалидны) |
| `Market closed` | Рынок закрыт в это время |

---

### 📊 POSITION
**Обновление позиции**

```
📊 POSITION | Symbol=BTCUSDT | Direction=BUY | 
Size=0.05 | EntryPrice=43250.50 | CurrentPrice=43300.00 | 
PnL=25.00
```

**Что это значит:**
- Позиция открыта
- Текущая прибыль: $25

---

## 🔧 Как добавить логирование в свой код

### Использование signal_logger в стратегиях

```python
from signal_logger import get_signal_logger

logger = get_signal_logger()

# Логировать сгенерированный сигнал
logger.log_signal_generated(
    strategy_name="MyStrategy",
    symbol="BTCUSDT",
    direction="BUY",
    confidence=0.75,
    price=43250.50
)

# Логировать отклоненный сигнал
logger.log_signal_rejected(
    strategy_name="MyStrategy",
    symbol="BTCUSDT",
    direction="BUY",
    confidence=0.75,
    reason="Volatility filter failed",
    volatility=0.045,
    threshold=0.02
)

# Логировать проверку фильтра
logger.log_filter_check(
    filter_name="SpreadFilter",
    symbol="BTCUSDT",
    passed=False,
    value=5.37,
    threshold=2.0
)
```

---

## 📁 Файлы логов

Логи сохраняются в папке `logs/`:

```
logs/
  signals_2026-02-04.log    ← сегодняшние логи
  signals_2026-02-03.log    ← вчерашние логи
  signals_2026-02-02.log    ← старые логи
  bot_2026-02-04.log        ← основные логи бота
```

**Ротация:** Когда файл достигает 50MB, создается новый файл (до 10 файлов).

---

## 🎯 Типичный процесс отладки

### Сценарий: "Ордера не открываются"

**Шаг 1:** Откройте Dashboard → "Логи Сигналов"

**Шаг 2:** Обновите страницу и зафиксируйте

**Шаг 3:** Смотрим логи:

**Если видим ✅ SIGNAL GENERATED но нет ❌ SIGNAL REJECTED:**
- Сигнал сгенерирован, но ордер не открыт
- Причина: фильтр отклонил
- Решение: уменьшите пороги в Settings или отключите фильтры

**Если видим ❌ SIGNAL REJECTED:**
- Читаем "Reason" в логе
- Например: `Spread filter check failed`
- Решение: в Settings увеличьте лимит спреда

**Если видим ⏳ ORDER EXEC START но нет ✅ ORDER EXEC SUCCESS:**
- Ордер отправлен, но не выполнен
- Причина: ошибка API или недостаточно баланса
- Решение: проверьте "Error" в логе

---

## 🎬 Примеры реальных ситуаций

### Ситуация 1: Много сигналов, но ордера не открываются

```
✅ SIGNAL GENERATED | Strategy=TrendPullback | Confidence=0.65
❌ SIGNAL REJECTED | Reason=Spread filter check failed | spread=5.37, limit=2.0
```

**Решение:** Увеличьте лимит спреда в Settings (уменьшите требования)

---

### Ситуация 2: Никаких сигналов вообще

```
(пусто)
```

**Возможные причины:**
1. Стратегия отключена в конфиге
2. Условия стратегии не выполняются
3. ADX/другие индикаторы не готовы

**Решение:** 
- Проверьте `active_strategies` в config
- Уменьшите пороги в AGGRESSIVE_TESTNET

---

### Ситуация 3: Ордера закрываются слишком быстро

```
✅ ORDER EXEC SUCCESS | OrderID=123 | FilledQty=0.05 | Price=43250
❌ ORDER EXEC FAILED | Direction=SELL | Reason=Price moved away
```

**Решение:** Увеличьте `take_profit_percent` в Settings

---

## 🔐 Безопасность

Логи содержат чувствительную информацию (размеры позиций, цены). 

**Как защитить:**
- Сохраняйте логи в защищенной папке
- Не делитесь логами публично
- Удаляйте старые логи регулярно (удаление файлов в `logs/`)

---

## 🐛 Когда логирование поможет

✅ Используйте для:
- Отладки отсутствия ордеров
- Понимания причин отклонения сигналов
- Анализа проблем с выполнением
- Проверки уровня волатильности
- Мониторинга спреда

❌ НЕ используйте для:
- Анализа исторических данных (используйте истории)
- Стратегирования (используйте бэктесты)

---

## 📞 Шпаргалка

| Ситуация | Где смотреть | Что проверить |
|----------|---------|----------|
| Ордера не открываются | Логи сигналов | ❌ SIGNAL REJECTED |
| Ордер отклонен биржей | Логи сигналов | ❌ ORDER EXEC FAILED |
| Баланс изменился неправильно | Вкладка Аккаунт | Balance в реальном времени |
| Неправильный размер позиции | Логи сигналов | 📊 POSITION |
| Спред слишком большой | Логи сигналов | Details в SIGNAL REJECTED |

---

## 🎓 Рекомендации для тестирования

1. **Используйте AGGRESSIVE_TESTNET** - много сигналов для тестирования
2. **Включите логирование** - всегда видеть что происходит
3. **Проверяйте логи каждый час** - ловите проблемы рано
4. **Документируйте** - сохраняйте логи интересных случаев
5. **Анализируйте тренды** - ищите паттерны в отклонениях

---

**Версия:** 1.0  
**Дата:** 2026-02-04  
**Статус:** Ready for Debugging
