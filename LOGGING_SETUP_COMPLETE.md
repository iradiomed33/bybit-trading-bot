# ✅ ЛОГИРОВАНИЕ СИГНАЛОВ - ПОЛНОСТЬЮ НАСТРОЕНО

## 🎉 Что было добавлено

### 1. Полная система логирования сигналов
- ✅ **signal_logger.py** - специальный модуль для логирования
- ✅ **logs/signals_YYYY-MM-DD.log** - файл логов (автоматически создается)
- ✅ API эндпоинт `/api/signals/logs` - получение логов через браузер
- ✅ **Вкладка Dashboard** - "Логи Сигналов" 🐛

### 2. Логирование в торговом боте
- ✅ **bot/trading_bot.py** - логирует каждый анализ рынка
- ✅ Когда есть сигнал → `✅ SIGNAL GENERATED`
- ✅ Когда сигнал принят → `✅ SIGNAL ACCEPTED`
- ✅ Когда сигнал отклонен → `❌ SIGNAL REJECTED` (с причиной)
- ✅ Отладочная информация → `🔍 DEBUG`

### 3. Документация
- ✅ **SIGNAL_LOGGING.md** - полный гайд логирования
- ✅ **QUICK_START.md** - обновлен с инструкциями
- ✅ **start_trading_bot.bat** - быстрый запуск бота

---

## 🚀 Как использовать

### Запустить логирование:

**Терминал 1:**
```bash
python run_api.py
```

**Терминал 2:**
```bash
echo "NO" | python cli.py paper
```

### Просмотреть логи:

1. Откройте http://localhost:8000
2. Вкладка "Логи Сигналов"
3. Нажмите "Обновить"

### Что вы увидите:

✅ **SIGNAL GENERATED** - стратегия нашла условие
✅ **SIGNAL ACCEPTED** - ордер открыт
❌ **SIGNAL REJECTED** - сигнал отклонен (причина указана)
🔍 **DEBUG** - анализ рынка каждые ~10 сек

---

## 📊 Пример логов

```
2026-02-04 20:05:29 | DEBUG    | 🔍 DEBUG | Category=market_analysis | {"symbol": "BTCUSDT", "last_close": 75011.3, "no_signal_reason": "No strategy triggered"}

2026-02-04 20:09:23 | INFO     | ✅ SIGNAL GENERATED | Strategy=TrendPullback | Symbol=BTCUSDT | Direction=BUY | Confidence=0.65 | Price=43250.50

2026-02-04 20:09:24 | INFO     | ✅ SIGNAL ACCEPTED | Strategy=TrendPullback | Symbol=BTCUSDT | Direction=BUY | Confidence=0.65

2026-02-04 20:09:25 | ERROR    | ❌ ORDER EXEC FAILED | Symbol=BTCUSDT | Direction=BUY | Reason=Insufficient balance
```

---

## 🔍 Для отладки

**Если ордера не открываются:**
1. Смотрите вкладку "Логи Сигналов"
2. Ищите **❌ SIGNAL REJECTED** или **❌ ORDER EXEC FAILED**
3. Читайте "Reason" в логе
4. Исправьте параметр в Settings

**Если нет сигналов:**
1. Проверьте что оба терминала работают
2. Снизьте confidence_threshold в Settings (0.60 → 0.35)
3. Выключите volatility_filter_enabled
4. Подождите 5-10 минут

---

## 📁 Файлы логов

```
logs/
  signals_2026-02-04.log      ← Сегодняшние сигналы
  signals_2026-02-03.log      ← Вчерашние сигналы
  bot_2026-02-04.log          ← Основные логи бота
```

Логи ротируются автоматически (50MB на файл, до 10 файлов).

---

## ✅ Проверка что работает

```powershell
# Проверить что файл логов существует
Get-Content logs/signals_*.log -Tail 5

# Проверить есть ли сигналы (ищем SIGNAL)
Select-String "SIGNAL" logs/signals_*.log
```

---

## 🎯 Следующие шаги

1. **Загрузить агрессивные настройки:**
   ```powershell
   Copy-Item config\bot_settings_AGGRESSIVE_TESTNET.json config\bot_settings.json -Force
   ```

2. **Запустить обе программы:**
   - Терминал 1: `python run_api.py`
   - Терминал 2: `echo "NO" | python cli.py paper`

3. **Мониторить логи:**
   - http://localhost:8000 → Логи Сигналов

4. **Ждать сигналов:**
   - Обычно 2-5 минут
   - Зависит от волатильности рынка

---

**Логирование готово к использованию!** 🚀
