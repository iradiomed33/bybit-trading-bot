# 🔧 Исправление: Настройки из UI теперь влияют на бота

## 📋 Описание проблемы

**Симптом**: Настройки, изменённые через веб-интерфейс, не влияли на работу бота.

**Причина**: Бот читает большинство параметров конфигурации **только при запуске** (в методе `__init__`). Когда UI обновляет настройки:
1. Конфиг обновляется в памяти (`config.set()`)
2. Конфиг сохраняется в файл (`config.save()`)
3. **НО** запущенный бот продолжает использовать **старые значения**

Например:
- `risk_management.max_leverage` - читается при создании RiskLimits
- `stop_loss_tp.sl_atr_multiplier` - читается при создании StopLossManager
- `meta_layer.use_mtf` - читается при создании MetaLayer

Все эти значения были прочитаны **один раз** при запуске бота и больше не обновляются.

---

## ✅ Решение

### 1. Добавлен `config.reload()` после сохранения

```python
config.set(key, value)
config.save()
config.reload()  # ← НОВОЕ: перезагружаем из файла
```

Это гарантирует что память и файл синхронизированы.

### 2. Автоматический перезапуск бота

При изменении **критичных параметров**, бот автоматически перезапускается:

```python
# Параметры, требующие перезапуска:
restart_required_prefixes = [
    "risk_management.",      # Риск-менеджмент
    "risk_monitor.",         # Мониторинг рисков
    "stop_loss_tp.",         # Стоп-лоссы и тейк-профиты
    "paper_trading.",        # Paper trading настройки
    "execution.",            # Исполнение ордеров
    "meta_layer.",           # MetaLayer (стратегии, режимы)
    "no_trade_zone.",        # No-trade зоны
    "kill_switch.",          # Kill switch
]

if requires_restart and bot_is_running:
    await stop_bot()         # Останавливаем
    await asyncio.sleep(1.0) # Пауза
    await start_bot()        # Запускаем с новым конфигом
```

### 3. Расширенный ответ API

API endpoint `/api/config/{key}` теперь возвращает:

```json
{
  "status": "success",
  "message": "Config updated: risk_management.max_leverage = 10 (bot automatically restarted)",
  "requires_restart": true,
  "bot_restarted": true
}
```

- `requires_restart` - требует ли этот параметр перезапуска бота
- `bot_restarted` - был ли бот автоматически перезапущен

### 4. WebSocket уведомления

UI получает обновление через WebSocket:

```json
{
  "type": "config_updated",
  "key": "risk_management.max_leverage",
  "value": 10,
  "config": { /* полный конфиг */ },
  "requires_restart": true,
  "bot_restarted": true
}
```

---

## 🎯 Как это работает

### До исправления ❌

```
┌─────┐  POST /api/config/risk_management.max_leverage  ┌────────┐
│ UI  │ ────────────────────────────────────────────────> │  API   │
└─────┘                                                   └────────┘
                                                               │
                                                               ▼
                                                         config.set()
                                                         config.save()
                                                               │
┌─────────┐                                                   │
│   БОТ   │ ◄─────────────────────────────────────────────────┘
│ (старое │   Конфиг обновлён, но бот использует
│  значе- │   старое значение из __init__
│   ние)  │   
└─────────┘   ❌ Изменения НЕ применены
```

### После исправления ✅

```
┌─────┐  POST /api/config/risk_management.max_leverage  ┌────────┐
│ UI  │ ────────────────────────────────────────────────> │  API   │
└─────┘                                                   └────────┘
                                                               │
                                                               ▼
                                                         config.set()
                                                         config.save()
                                                         config.reload() ← НОВОЕ
                                                               │
                                                               ▼
                                                         requires_restart?
                                                               │ YES
                                                               ▼
                                                         stop_bot()
                                                         sleep(1.0)
                                                         start_bot()
                                                               │
┌─────────┐                                                   │
│   БОТ   │ ◄─────────────────────────────────────────────────┘
│ (НОВОЕ  │   Бот перезапущен с новым конфигом
│  значе- │   
│   ние)  │   ✅ Изменения применены!
└─────────┘
```

---

## 📝 Примеры использования

### Изменение leverage через UI

1. Открываете UI → Settings → Risk Management
2. Меняете Max Leverage с 10 на 5
3. Нажимаете Save

**Что происходит:**
- API получает `POST /api/config/risk_management.max_leverage` с value=5
- Конфиг обновляется: `config.set("risk_management.max_leverage", 5)`
- Конфиг сохраняется: `config.save()`
- Конфиг перезагружается: `config.reload()`
- Определяется что требуется перезапуск: `"risk_management." in restart_required_prefixes`
- Бот автоматически останавливается и перезапускается
- UI показывает: "✅ Config updated (bot automatically restarted)"

### Изменение неважного параметра

1. Меняете `logging.level` с INFO на DEBUG
2. Нажимаете Save

**Что происходит:**
- Конфиг обновляется и сохраняется
- Перезапуск **НЕ** требуется (не в списке prefixes)
- UI показывает: "✅ Config updated"

---

## 🧪 Тесты

Добавлены тесты в `tests/test_ui_config_sync.py`:

```bash
# Запустить тесты
pytest tests/test_ui_config_sync.py -v

# Результат:
# ✅ test_config_set_and_save_updates_memory - PASSED
# ✅ test_config_reload_after_save - PASSED
# ✅ test_global_config_instance_persistence - PASSED
# ✅ test_config_changes_requiring_restart - PASSED
```

---

## 🔍 Технические детали

### Параметры, требующие перезапуска

| Префикс | Описание | Примеры |
|---------|----------|---------|
| `risk_management.*` | Управление рисками | max_leverage, max_notional, daily_loss_limit_percent |
| `risk_monitor.*` | Мониторинг рисков | max_daily_loss_percent, max_position_notional |
| `stop_loss_tp.*` | SL/TP настройки | sl_atr_multiplier, tp_atr_multiplier |
| `paper_trading.*` | Paper trading | initial_balance, maker_commission |
| `execution.*` | Исполнение ордеров | order_type, time_in_force |
| `meta_layer.*` | MetaLayer | use_mtf, mtf_score_threshold |
| `no_trade_zone.*` | No-trade зоны | max_atr_pct, max_spread_pct |
| `kill_switch.*` | Kill switch | max_consecutive_errors, cooldown_minutes |

### Параметры, НЕ требующие перезапуска

- `trading.symbol` - изменение символа (требует ручного перезапуска)
- `trading.mode` - paper/live (требует ручного перезапуска)
- `logging.level` - уровень логирования
- `api.*` - настройки API

---

## ⚠️ Важные замечания

1. **Автоматический перезапуск работает только если бот запущен**
   - Если бот остановлен, изменения просто сохраняются
   - При следующем запуске бот использует новые значения

2. **Перезапуск занимает ~1-2 секунды**
   - Бот корректно останавливается
   - Пауза 1 секунда для чистой остановки
   - Бот запускается с новым конфигом

3. **WebSocket подключение сохраняется**
   - UI продолжает получать обновления
   - Логи не прерываются

4. **Открытые позиции НЕ закрываются**
   - Бот останавливает только loop
   - Позиции остаются на бирже
   - После перезапуска бот продолжает управлять ими

---

## 🚀 Обратная совместимость

✅ **Полная обратная совместимость** с существующим кодом:
- Все API endpoints работают как раньше
- Поведение для не-критичных параметров не изменилось
- Старый код продолжает работать без изменений

---

## 📊 Статистика изменений

- **Файлов изменено**: 2
  - `api/app.py` - обновлена функция `update_config()`
  - `tests/test_ui_config_sync.py` - добавлены тесты
- **Строк добавлено**: ~230
- **Тестов добавлено**: 4 (все проходят)

---

## ✨ Итог

**Проблема решена!** Теперь настройки из UI корректно применяются к боту:
- ✅ Критичные параметры автоматически перезапускают бот
- ✅ UI получает обратную связь о перезапуске
- ✅ Конфиг синхронизирован между памятью и файлом
- ✅ Полная обратная совместимость

**Пользователь видит**: Изменил настройку → Бот перезапустился → Настройка применена! 🎉
